# 配置一致性总核对（2026-02-28）

## 结论
- P0 安全相关新增变量已实现“代码、模板、编排、文档”四层一致。
- 当前状态可用于答辩说明与上线交接，且已通过回归验证链路。

## 本次核对变量
- `CSRF_EXEMPT_PATH_PREFIXES`
- `ENABLE_RATE_LIMIT`
- `RATELIMIT_ENABLE`
- `TRUST_PROXY_HEADERS`

## 一致性检查结果

### 1) 代码层（Django Settings）
- `server/settings.py` 已支持并读取上述变量。
- 测试进程默认策略已收敛：
  - `ENABLE_RATE_LIMIT` 默认关闭（测试态）
  - `RATELIMIT_ENABLE` 默认关闭（测试态）
  - `ENABLE_PERFORMANCE_MONITOR` 默认关闭（测试态）

### 2) 模板层（环境变量示例）
- `source/env.example` 已包含 4 个变量及注释。

### 3) 编排层（Docker Compose）
- `docker-compose.yml` backend 环境段已包含 4 个变量。
- `docker-compose.prod.yml` backend 环境段已包含 4 个变量（支持 `${VAR:-default}`）。
- `dockerpull/docker-compose.yml` backend 环境段已同步包含 4 个变量。

### 4) 文档层（部署/验收/运行）
- `source/docs/P0安全基线上线清单.md`：已包含变量示例、风险边界与增量修复记录。
- `source/docs/部署验收清单_答辩版.md`：已包含变量示例与代理头误配风险项。
- `source/server/README.md`：已补充限流双开关与代理头信任建议。
- `DOCKER_DEPLOYMENT.md`：已补充 compose 示例与安全建议。

## 已完成验证
- `python manage.py check`：通过
- `python tools/run_regression_baseline.py`：通过（4/4）
- 管理端广覆盖回归：通过（23/23）
- 中间件新增单测：通过（8/8）

## 答辩可直接口播（20秒）
- “我们这轮不是只改代码，而是把配置一致性做到端到端。”
- “同一组安全变量已在 settings、env 示例、三套 compose、部署与验收文档全部对齐。”
- “同时通过了系统检查、基线回归和管理端广覆盖回归，保证改动可复现、可交付。”
