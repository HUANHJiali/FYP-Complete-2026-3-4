# 答辩展示顺序（60秒脚本）

## 展示目标
- 60秒内证明：我们做的是“可上线、可回归、可交付”的安全与配置收敛。

## 展示顺序（按文件打开）

### 第1步（10秒）
打开：`source/docs/P0安全基线上线清单.md`

口播：
- “这是我们的 P0 安全基线清单，核心是最小改动、可回滚、可验证。”
- “这里可以看到关键安全开关和上线前核查项。”

### 第2步（15秒）
打开：`source/docs/配置一致性总核对_20260228.md`

口播：
- “这份是一页总核对，代码层、模板层、编排层、文档层四层一致。”
- “同一组变量在 settings、env、compose、部署文档都已对齐。”

### 第3步（15秒）
打开：`docker-compose.prod.yml`

口播：
- “这里是生产编排，安全变量已显式透传：限流双开关、CSRF路径白名单、代理头信任边界。”
- “默认策略是安全收敛，只有明确场景才放开。”

### 第4步（10秒）
打开：`source/server/server/settings.py`

口播：
- “后端设置里已实现测试态与生产态分离，测试默认关闭限流和性能监控，避免误报。”
- “生产环境仍通过环境变量保持安全策略开启。”

### 第5步（10秒）
展示终端结果：
- `python manage.py check`
- `python tools/run_regression_baseline.py`

口播：
- “最后用系统检查和基线回归收口，确保改动不影响核心链路。”

## 结束语（可选，10秒）
- “我们这轮不是只修代码，而是把配置、编排、文档和验证证据做成了可复现闭环。”
