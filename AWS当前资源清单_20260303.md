# AWS 当前资源清单（2026-03-03）

## 1. 部署概览

- 区域：`us-east-1`
- 部署方式：Terraform（HA 模式）
- 当前状态：运行中（前端/API 健康检查通过）

## 2. 当前已创建资源（实测）

### 2.1 计算与网络入口

1. **EC2 实例（2台）**
   - `i-0a447157026ed7ca1`（`fyp-backend-1`）
   - `i-0d78a38def6956b92`（`fyp-backend-2`）
   - 规格：`t2.micro`
   - 可用区：`us-east-1f`
   - 公网 IP：`3.236.144.212`、`3.229.113.224`

2. **应用负载均衡器（ALB，1个）**
   - 名称：`fyp-exam-system-alb`
   - 状态：`active`
   - 类型：`application`
   - 入口地址：`http://fyp-exam-system-alb-1914926535.us-east-1.elb.amazonaws.com`

3. **安全组（1个）**
   - 名称：`fyp-exam-system-sg`
   - ID：`sg-013951c1c55b40b82`
   - 作用：开放 SSH/HTTP/HTTPS/API（按 Terraform 规则）

4. **Key Pair（1个）**
   - 名称：`fyp-keypair`
   - 类型：`ed25519`

### 2.2 负载均衡与路由

1. **目标组（2个）**
   - `fyp-frontend-tg`：端口 `80`，健康检查路径 `/`
   - `fyp-backend-tg`：端口 `8000`，健康检查路径 `/api/health/simple/`

2. **监听与规则**
   - Listener：`80/HTTP`
   - Rule：`/api/*` 路径转发到后端目标组，其余流量到前端目标组

3. **目标绑定（4个）**
   - 前端绑定 2 条（两台实例）
   - 后端绑定 2 条（两台实例）

## 3. Terraform State 对应项

当前 State 包含（核心资源）：

- `aws_instance.fyp_backend[0]`
- `aws_instance.fyp_backend[1]`
- `aws_lb.fyp_alb[0]`
- `aws_lb_listener.fyp_main[0]`
- `aws_lb_listener_rule.fyp_api[0]`
- `aws_lb_target_group.fyp_frontend_tg[0]`
- `aws_lb_target_group.fyp_backend_tg[0]`
- `aws_lb_target_group_attachment.fyp_frontend[0..1]`
- `aws_lb_target_group_attachment.fyp_backend[0..1]`
- `aws_security_group.fyp_sg`
- `aws_key_pair.fyp_key`

## 4. 未创建资源（本次）

- **RDS 未创建**（输出显示：`N/A (RDS not created)`）
- **未新建 VPC/Subnet**（使用 AWS 默认 VPC 与默认子网）

## 5. 当前对外可访问地址

- 前端：`http://fyp-exam-system-alb-1914926535.us-east-1.elb.amazonaws.com`
- API：`http://fyp-exam-system-alb-1914926535.us-east-1.elb.amazonaws.com/api/`
- API 健康检查：`http://fyp-exam-system-alb-1914926535.us-east-1.elb.amazonaws.com/api/health/simple/`

## 6. 成本与建议

- 长挂会持续计费（EC2 + ALB + 公网 IPv4 + EBS）。
- 演示完成后建议立即执行：

```powershell
cd infrastructure/terraform
.\set-env.ps1
terraform destroy -auto-approve -input=false -var=enable_ha=true
```

---

更新时间：2026-03-03（基于当前 AWS 实时查询结果）
