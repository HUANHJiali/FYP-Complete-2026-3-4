# FYP_230180642_HUANG Jiali 可复制修改稿（2026-02-25）

> 用法：按“替换位置”在原 Word 文档中查找对应段落，整段替换为本稿文本。

---

## 1) Project Overview（整段替换）

### 替换位置
原文 `Overview` 段落（含 Frontend/Backend/Database/AI/Features 一段）

### 替换文本（可直接复制）
This project delivers an intelligent online examination platform with a role-based workflow for administrators, teachers, and students.

**Technology Stack**
- Frontend: Vue 3, Vue Router 4, Vuex 4, View UI Plus, Axios
- Backend: Django 4.x (REST-style APIs)
- Database: MySQL 8.x
- AI Layer: ZhipuAI GLM-4-Flash (OpenAI-compatible API)
- Deployment: Docker Compose (recommended), with optional Nginx + Gunicorn for production

**Core Features**
- Secure login and role-based access control
- Question bank management with CSV import/export
- Practice papers, tasks, exam workflows, and wrong-question drills
- AI-assisted scoring and AI question generation
- Unified API response contract and robust frontend interceptor handling

The system focuses on scalability, faster teacher feedback cycles, and better student learning outcomes.

---

## 2) Objectives（整段替换）

### 替换位置
原文 `Objectives` 的项目符号列表

### 替换文本（可直接复制）
- Provide practical AI-assisted scoring with explainable feedback and fallback handling.
- Enable automated question generation by subject/topic/difficulty to expand the item bank.
- Support complete multi-role workflows (admin/teacher/student) with token-based authentication.
- Improve reliability through standardized API responses and defensive error handling.
- Deliver a demo-ready system with reproducible deployment and clear evidence documentation.

---

## 3) Non-functional Requirements（整段替换，重点修正技术栈冲突）

### 替换位置
原文 `Non-functional requirements Hardware List / Software/platform List / Cloud Service List`

### 替换文本（可直接复制）
### Non-functional Requirements

**Hardware**
- Development PC/Laptop (Windows/macOS/Linux)
- One server/VM for production deployment (optional for final demo)

**Software/Platform**
- Python 3.9+
- Node.js 16+
- Django 4.x
- Vue 3
- MySQL 8.x
- Docker & Docker Compose
- GitHub (version control)
- VS Code or PyCharm (development IDE)

**Quality Attributes**
- Availability: service-level health checks for frontend/backend
- Security: token authentication, input validation, and error sanitization
- Maintainability: modularized views and documented API endpoints
- Performance: acceptable response latency for key read/write operations

**Cloud/Infrastructure (Optional Extension)**
- Terraform-based infrastructure provisioning (if cloud deployment is required)
- Nginx reverse proxy + Gunicorn application hosting

> 注：删除原文中与当前项目不一致的项（如 Flask、DynamoDB、Elasticsearch、Lambda 等），避免答辩被追问。

---

## 4) Critical Evaluation（整段替换）

### 替换位置
原文 `8. Critical Evaluation`

### 替换文本（可直接复制）
## 8. Critical Evaluation

### 8.1 Problems Encountered and Solutions
1. **API inconsistency across legacy modules**
   - Issue: Different endpoints and response handling styles caused integration friction.
   - Solution: Unified response contract (`code/msg/data`) and aligned frontend interceptors.

2. **Workflow failures in high-risk write operations**
   - Issue: Message/task/wrong-question/exam publishing flows had compatibility defects.
   - Solution: Applied targeted backend fixes and completed regression closure with `code=0` results.

3. **Deployment reproducibility gaps**
   - Issue: Manual startup steps were error-prone.
   - Solution: Enhanced Docker startup scripts with readiness waiting, health checks, login self-test, and failure log output.

### 8.2 Current Limitations
- AI evaluation consistency still needs repeated-sample statistical validation.
- Long-duration health monitoring data is not yet fully populated in KPI evidence.
- Some documentation still requires final data fill-in (man-hours, commit mapping, meeting logs).

### 8.3 Improvement Plan
- Complete KPI data collection (P95 latency, AI consistency, health availability time series).
- Finalize team contribution matrix with auditable commit evidence.
- Prepare final demo script and Q&A evidence mapping for defense.

---

## 5) Project Progress and Schedule（整段替换）

### 替换位置
原文 `9. Project Progress and Schedule`

### 替换文本（可直接复制）
## 9. Project Progress and Schedule

### 9.1 Completed Milestones
- W01-W02: Requirement clarification and architecture/database baseline
- W03-W04: Core API development and frontend integration
- W05-W08: AI integration, exam/task/wrong-question workflow completion
- W09-W10: Docker automation and deployment hardening
- W11-W12: Regression closure, evidence packaging, and defense preparation

### 9.2 Current Deliverables
- Multi-role functional system with complete key workflows
- Docker-based reproducible deployment and startup self-check
- Regression checklist, evidence index, weekly logs (W01-W12), KPI template, and defense scripts

### 9.3 Remaining Actions Before Final Submission
- Fill final KPI measured values and attach raw evidence
- Fill contribution matrix with verified commit records
- Freeze final report formatting (TOC/page numbers/reference consistency)

---

## 6) References（整段替换）

### 替换位置
原文 `10. References`

### 替换文本（可直接复制）
## 10. References
1. Django Documentation: https://docs.djangoproject.com/
2. Vue 3 Documentation: https://vuejs.org/
3. Axios Documentation: https://axios-http.com/
4. Docker Documentation: https://docs.docker.com/
5. MySQL Documentation: https://dev.mysql.com/doc/
6. ZhipuAI Open Platform: https://open.bigmodel.cn/

---

## 7) 立即删除或改写的高风险原文（必须处理）

### A. 删除这类错误技术项
- `Flask`
- `Vote3` / `View3`（拼写与事实都不准确）
- `Amazon DynamoDB`
- `Amazon Elasticsearch Service`
- `Amazon Lambda`
- 与当前实现无关的云服务清单

### B. 删除这类“会被老师质疑”的伪代码片段
- 含明显语法错误/拼写错误的代码（如 `Straw(token)`, `request.Post.gate`, `Q)`）
- 与当前仓库实现不一致的历史片段

### C. 替换重复段落
- `Teacher/Student screens` 下重复三次的 message/dynamics/profile 说明
- 重复的 `students.vue / teachers.vue` 段落

---

## 8) 答辩时建议口径（可直接读）
We intentionally aligned the report with the implementation in the repository. Legacy or inconsistent items were removed, and all key claims are now evidence-linked through the Kickoff evidence index, regression checklist, weekly logs, KPI sheet, and defense scripts.

---

## 9) 你还需要继续修改的地方（必须做）

### 9.1 目录与编号（格式必修）
- 删除所有 `PAGEREF` 残留文本，重新生成目录与页码。
- 检查章节编号连续性（例如 7.7/7.8/7.9 前面缺失 7.1-7.6 的情况）。
- 统一标题级别（Heading 1/2/3），避免同级标题样式不一致。

### 9.2 数据表描述（术语与字段）
- 把不准确术语改为真实业务名称：
   - `chart of accounts` 改为 `Projects (Subjects)`
   - `class table` 改为 `Grades`
- 字段说明要与当前 Django 模型一致，避免“文档字段”和“数据库字段”不一致。

### 9.3 代码截图与伪代码（高风险）
- 删除所有明显错误代码（如拼写/语法错误样例）。
- 代码片段统一改成“关键逻辑伪代码 + 文件路径说明”，不要贴长段旧代码。
- 每个角色只保留 1~2 个最能代表价值的代码片段，避免重复。

### 9.4 测试数据口径（防追问）
- 在报告中明确区分：
   - 历史阶段测试结果（如 68%、79.3%）
   - 当前提交版本高风险回归结果（链路级全通过）
- 不要把不同时间、不同测试范围的通过率直接混成一个结论。

---

## 10) 建议新增的内容（可直接贴到报告）

### 10.1 Executive Summary（新增一节，放在目录后）
This project implements an AI-enhanced online examination platform with complete role-based workflows for administrators, teachers, and students. The final submission emphasizes three outcomes: reproducible deployment, workflow-level functional closure, and auditable delivery evidence. The system supports question management, practice and task learning loops, exam operations, wrong-question reinforcement, and AI-assisted scoring/question generation. Current deliverables include Docker startup automation with readiness checks, regression checklists for high-risk operations, weekly project logs, KPI templates, and defense scripts.

### 10.2 Requirements Traceability（新增一节）
| Requirement | Implementation | Evidence |
|---|---|---|
| Multi-role access control | Token-based auth + role menus | Kickoff evidence index + regression checklist |
| Exam/practice/task workflows | Django APIs + Vue pages | Role-based regression results |
| AI-assisted grading/generation | AI utility layer + frontend modules | AI usage docs + demo scripts |
| Reproducible deployment | Docker Compose + startup scripts | Health check and login self-test records |

### 10.3 UI/UX Evaluation（新增一节）
The UI follows a unified blue-theme visual language across login and core management pages. Improvements include consistent spacing, button semantics, table readability, and interaction feedback. Responsive behavior is supported for common viewport sizes. Remaining optimization focuses on reducing animation density in critical flows, strengthening accessibility (contrast and keyboard focus), and adding mobile evidence screenshots for formal review.

### 10.4 Security and Risk Controls（新增一节）
Security controls include token authentication, input validation, and error-handling hardening on high-risk write paths. Risk controls are documented using a closure loop: issue identification, targeted fix, and regression verification. Known risks include AI response variance and incomplete long-duration KPI measurements; mitigation actions are planned and tracked in KPI and weekly log documents.

### 10.5 Final Submission Checklist（新增附录）
- TOC and page numbers regenerated with no PAGEREF residue.
- All technology descriptions aligned with repository implementation.
- KPI sheet includes final measured values and raw evidence references.
- Contribution matrix includes man-hours and commit mapping.
- Weekly logs contain meeting notes and evidence links.
- Defense script and evidence index are cross-checkable.

---

## 11) 你可以直接补的两句“防追问”标准回答

### Q1: Why are there different pass rates in different reports?
Different reports were produced at different project stages and test scopes. We therefore separate historical stage results from the latest submission regression results to keep the evaluation methodologically consistent.

### Q2: How do you prove this version is deliverable?
We provide auditable evidence across deployment reproducibility, role-based workflow regression, weekly development logs, KPI templates, and defense scripts, all linked through a single evidence index.

---

## 12) Additional Implemented Features（可直接新增到正文）

> 用法：如果你原报告里“功能”写不全，可把以下小节直接追加到 `Features` 或 `System Modules` 章节。

### 12.1 Message Center Full Workflow
The system implements a complete messaging workflow across roles, including message creation, delivery, read-state tracking, reader-detail queries, and deletion. This module improves communication efficiency and provides traceability for announcements and learning notifications.

```javascript
// client/src/views/pages/messageCenter.vue
// 标记全部已读（前端状态）
this.messages.forEach(m => { m.isRead = true })

// 典型接口调用（封装在 api 层）
await markMessageRead({ messageId, token })
await deleteMessage({ messageId, token })
```

### 12.2 Task Center with Progress Persistence
Students can start tasks, save intermediate answers, and submit final results. The backend preserves task logs and answer records, enabling interrupted sessions to continue and supporting fair evaluation of learning progress.

```javascript
// client/src/views/pages/taskCenter.vue
// 开始任务并携带 logId 进入答题页
startTask(token, task.id).then(resp => {
   if (resp.code === 0) {
      this.$router.push({
         path: '/answer',
         query: { type: 'task', taskId: task.id, logId: resp.data.logId }
      })
   }
})
```

### 12.3 Wrong-Question Notebook and Review Loop
The platform supports wrong-question collection, detail viewing, review-history recording, reviewed-status marking, and wrong-question deletion. This closes the practice-feedback loop and strengthens targeted remediation.

```python
# server/app/views/wrong_question_views.py (逻辑示意)
def add_wrong_question(request):
   source_id = request.POST.get('sourceId')
   if not str(source_id).isdigit():
      return BaseView.warn('sourceId must be numeric')
   # 保存错题记录（含 wrongAnswer / correctAnswer / analysis）
   return BaseView.successData({'created': True})
```

### 12.4 Teacher Review and Score Publishing Control
For subjective items (e.g., fill-in and programming questions), teachers can perform item-level review before publishing final scores. A pre-publish validation check prevents score release when pending reviews still exist.

```javascript
// client/src/views/pages/teacherExamLogs.vue
// 发布前检查是否仍有待审核题目
checkAnswers(row.studentId, row.examId).then(resp => {
   if (resp.data.flag) {
      this.$Notice.warning({ title: '填空或编程未完全审核，不能发布成绩' })
   } else {
      putExamLog({ studentId: row.studentId, examId: row.examId })
   }
})
```

### 12.5 CSV Import/Export for Question and Practice Data
Administrators and teachers can batch-import question banks and export datasets for analysis or archival. This improves maintainability of large question pools and reduces manual data-entry effort.

```javascript
// client/src/views/pages/adminQuestions.vue
// CSV 导入
const fd = new FormData()
fd.append('file', file)
fd.append('subjectId', this.searchProject || '')
const resp = await importAdminQuestions(fd)

// CSV 导出
const down = await exportAdminQuestions(filters)
const blob = new Blob([down.data || down], { type: 'text/csv;charset=utf-8;' })
```

### 12.6 Operation Log Readability and Audit Support
The system includes paginated operation-log retrieval and filtering support for management-side auditing. This improves post-event diagnosis and supports accountability requirements in academic systems.

```python
# server/app/views.py (逻辑示意)
def logs_page(request):
   page_index = int(request.GET.get('pageIndex', 1))
   page_size = int(request.GET.get('pageSize', 10))
   # 支持按操作人/模块/时间范围筛选
   return BaseView.successData(BaseView.parasePage(page_index, page_size, page_total, count, data))
```

### 12.7 Docker Startup Automation with Health Verification
The deployment workflow is automated with container startup, readiness waiting, health checks, and login self-test. Failure paths provide service log tails for faster troubleshooting and reproducible recovery.

```yaml
# docker-compose.yml (关键片段)
backend:
   healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health/simple/"]

frontend:
   depends_on:
      backend:
         condition: service_healthy
```

```bat
:: docker-start.bat (关键片段)
call :wait_backend || goto :dump_logs
call :wait_frontend || goto :dump_logs
call :check_login  || goto :dump_logs
```

### 12.8 AI Interface Compatibility Hardening
AI-related endpoints include compatibility handling for parameter naming differences and safer fallback logic. This reduces integration fragility and keeps AI-enhanced features usable under partial failure conditions.

```python
# server/app/views/ai_views.py (逻辑示意)
wrong_answer = request.POST.get('wrong_answer') or request.POST.get('wrongAnswer')
result = AIUtils.ai_analyze_wrong_answer(
   question_content=question,
   wrong_answer=wrong_answer,
   correct_answer=correct
)
return BaseView.successData(result)
```

---

## 13) Optional One-Paragraph Insert for “System Highlights”

Beyond standard exam functions, this project has implemented production-oriented enhancements including end-to-end messaging, task progress persistence, wrong-question remediation loops, teacher-controlled subjective review, CSV-based data exchange, auditable operation logs, and Docker-based startup automation with health verification. These additions improve robustness, maintainability, and real-world deployability compared with a basic classroom prototype.

---

## 14) Role-based Functions

### 14.1 Administrator Functions
- User and organization management: colleges, grades, students, teachers, and role-linked accounts.
- Question bank management: create/edit/delete questions, type management, CSV import/export.
- Exam and task governance: create exams/tasks, monitor execution status, and maintain configuration consistency.
- Message administration: publish notifications, check read status, and manage message lifecycle.
- System analytics and logs: view dashboard statistics and operation logs for audit and diagnosis.
- Deployment readiness support: verify service health and basic login availability after startup.

### 14.2 Teacher Functions
- Exam supervision: view teaching-side exam logs and monitor student progress.
- Subjective-answer review: review fill-in/programming answers at item level before score release.
- Score publishing control: publish final scores only after pending review checks pass.
- Teaching content support: manage question-related content and use AI-assisted question generation.
- Learning feedback support: use AI scoring tools for fast draft evaluation and explanation.

### 14.3 Student Functions
- Exam participation: access assigned exams and submit responses within schedule constraints.
- Practice workflow: start/continue practice sessions with saved progress and result tracking.
- Task center: start tasks, save intermediate answers, and complete final submission.
- Wrong-question remediation: collect wrong answers, review explanations, and run targeted retry practice.
- Message and profile center: read announcements, track personal activity, update profile, and change password.
- Learning export support: export selected practice records for personal review.

### 14.4 Short Version Table (Optional)
| Role | Core Responsibilities |
|---|---|
| Administrator | Governance, configuration, analytics, audit, and data operations |
| Teacher | Review, score release control, teaching support, and AI-assisted evaluation |
| Student | Exam/practice/task participation, remediation, and personal learning management |

---

## 15) Administrator Task Management and Operation Logs（可直接贴）

### 15.1 Administrator Task Management
The administrator task-management module supports full lifecycle control of task entities, including creation, query, and deletion. Administrators can configure task metadata and assign tasks to target classes or users through backend APIs. This module ensures teaching tasks are centrally managed and traceable.

**Key implementation points**
- Task creation endpoint validates required fields before persistence.
- Task list endpoint supports pagination for management scalability.
- Task deletion endpoint supports controlled cleanup and immediate UI refresh.

```javascript
// 管理员侧任务典型调用（前端逻辑示意）
await addTask({ name, projectId, gradeId, deadline, token })
const page = await getTasksPage({ pageIndex, pageSize, keyword, token })
await deleteTask({ id, token })
```

```python
# 后端任务分页（逻辑示意）
def task_page(request):
      page_index = int(request.GET.get('pageIndex', 1))
      page_size = int(request.GET.get('pageSize', 10))
      data = Tasks.objects.all().order_by('-id')
      # 省略分页细节
      return BaseView.successData(BaseView.parasePage(page_index, page_size, page_total, count, rows))
```

### 15.2 Operation Log Management
The operation-log module provides auditable records for key management actions. Administrators can retrieve logs by page, filter by operator/module/time range, and use the result set for troubleshooting and compliance review.

**Key implementation points**
- Log records are structured and queryable for post-event analysis.
- Pagination reduces UI load and improves readability for long histories.
- Filter conditions support rapid location of abnormal operations.

```python
# 操作日志分页与筛选（逻辑示意）
def logs(request):
      page_index = int(request.GET.get('pageIndex', 1))
      page_size = int(request.GET.get('pageSize', 10))
      operator = request.GET.get('operator')
      module = request.GET.get('module')
      start_time = request.GET.get('startTime')
      end_time = request.GET.get('endTime')
      # 省略筛选与分页细节
      return BaseView.successData(BaseView.parasePage(page_index, page_size, page_total, count, rows))
```

```javascript
// 前端日志查询（逻辑示意）
const resp = await getAdminLogs({
   pageIndex: this.pageIndex,
   pageSize: this.pageSize,
   operator: this.filters.operator,
   module: this.filters.module,
   startTime: this.filters.startTime,
   endTime: this.filters.endTime
})
if (resp.code === 0) {
   this.pageInfos = resp.data.data
   this.totalInfo = resp.data.count
}
```

---

## 16) Student Data Visualization

### 16.1 Purpose and Value
The student data-visualization module presents learning outcomes in a clear and trackable way. It supports teachers and administrators in identifying performance trends, while helping students understand progress and weak areas.

### 16.2 Core Metrics
- Score trend over time (daily/weekly).
- Accuracy trend by time window.
- Distribution by subject/class.
- Learning activity counts (practice/task/exam participation).

### 16.3 Visualization Scenarios
- **Teacher view**: class-level trend chart and student comparison.
- **Administrator view**: overall student statistics and cohort insights.
- **Student view**: personal progress trend and completion summary.

### 16.4 Key Implementation Points
- Backend provides paginated/statistical APIs for trend aggregation.
- Frontend converts API payloads into chart-ready arrays.
- Charts support period filtering for fast diagnosis.

```javascript
// 前端：获取学生趋势数据并渲染图表（逻辑示意）
const resp = await getStudentStatistics({
   token: this.$store.state.token,
   studentId: this.currentStudentId,
   days: this.daysRange
})

if (resp.code === 0) {
   const xData = resp.data.trend.map(i => i.date)
   const scoreData = resp.data.trend.map(i => i.avgScore)
   const accData = resp.data.trend.map(i => i.accuracy)
   this.renderTrendChart(xData, scoreData, accData)
}
```

```python
# 后端：学生趋势统计（逻辑示意）
def statistics_student(request):
      student_id = request.GET.get('studentId')
      days = int(request.GET.get('days', 7))
      # 聚合练习/任务/考试日志，生成按日期的趋势数据
      trend = [
            {'date': '2026-02-20', 'avgScore': 78.0, 'accuracy': 0.72},
            {'date': '2026-02-21', 'avgScore': 82.5, 'accuracy': 0.79},
      ]
      return BaseView.successData({'trend': trend})
```

### 16.5 Practical Outcome
By visualizing score and accuracy trends, the system reduces analysis time for teachers and improves learning self-awareness for students. This module also strengthens data-driven decision-making in teaching management.
