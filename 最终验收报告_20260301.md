# 最终验收报告（Terraform HA 自动化部署）

- 项目：FYP 智能学生考试系统
- 验收日期：2026-03-01
- 验收环境：AWS Academy Learner Lab（us-east-1）
- 部署方式：Terraform 一键脚本（`infrastructure/terraform/learner-lab-deploy.ps1`）

## 一、验收目标

1. 在 AWS 上完成高可用部署（2 台 EC2 + ALB + 前后端目标组）。
2. 前端与 API 均可通过 ALB 正常访问。
3. 一键脚本具备可观测性和失败回滚能力（可通过参数控制是否保留资源）。

## 二、关键修复项（已落地）

1. **部署健康判定收紧**
   - 文件：`infrastructure/terraform/learner-lab-deploy.ps1`
   - 变更：成功条件由“前端或 API 任一成功”改为“前端和 API 同时成功”。

2. **前端端口与 ALB 对齐**
   - 文件：`infrastructure/terraform/main.tf`
   - 变更：在 `user_data` 中自动修正 `docker-compose.prod.yml` 前端映射为 `80:8080`，确保与前端 TG（80）一致。

3. **后端健康检查优化**
   - 文件：`infrastructure/terraform/main.tf`
   - 变更：后端 TG 健康检查路径改为 `/api/health/simple/`，降低启动阶段误判概率。

4. **Host Header 导致 400/502 问题修复**
   - 文件：`infrastructure/terraform/main.tf`
   - 变更：在实例初始化阶段补丁 `deploy/aws-deploy.sh` 并设置 `ALLOWED_HOSTS='*'`，兼容 ALB Host Header。

5. **迁移失败误报成功修复**
   - 文件：`deploy/aws-deploy.sh`
   - 变更：修复 `migrate` 退出码被管道吞掉的问题；迁移失败时明确 `exit 1`，避免假成功。

6. **Learner Lab 权限失效提示增强**
   - 文件：`infrastructure/terraform/learner-lab-deploy.ps1`
   - 变更：识别 `UnauthorizedOperation / voc-cancel-cred` 并输出明确操作指引（重开 Lab、刷新临时凭证）。

7. **CDK 脚本可用性增强（补充）**
   - 文件：`infrastructure/cdk/deploy-local.ps1`
   - 变更：新增 `-SkipBootstrap`，并在 bootstrap/deploy 失败时输出 Learner Lab 场景友好提示。

## 三、最终验收命令与结果

### 1) 一键部署命令

```powershell
Set-Location infrastructure/terraform
.\learner-lab-deploy.ps1 -SkipRollback -MaxHealthChecks 18 -HealthIntervalSec 20
```

### 2) 脚本执行结论

- `terraform validate`：通过
- `terraform apply`：成功（本轮资源无变更，仅同步输出）
- 健康检查：首轮即通过
  - Frontend status: `200`
  - API status: `200`
- 成功消息：`Deployment succeeded and both frontend/API are reachable.`

### 3) 目标组健康快照

- 前端目标组 `fyp-frontend-tg`：2/2 healthy（端口 80）
- 后端目标组 `fyp-backend-tg`：2/2 healthy（端口 8000）

### 4) 端到端访问快照

- 负载均衡地址：`http://fyp-exam-system-alb-867379207.us-east-1.elb.amazonaws.com`
- 前端首页：`HTTP 200`
- API 健康检查：`/api/health/simple/` 返回 `HTTP 200`，Body：`{"status": "ok"}`

## 四、当前可交付状态

- 系统已达到“可演示、可验收、可复现部署”的交付标准。
- Terraform 路径已稳定，推荐作为 Learner Lab 主交付方案。
- CDK 路径保留为补充方案（受 Learner Lab IAM 限制时可能无法 bootstrap）。

## 五、运维与回滚说明

1. 保留资源（用于演示）：

```powershell
.\learner-lab-deploy.ps1 -SkipRollback
```

2. 失败自动回滚（节省成本）：

```powershell
.\learner-lab-deploy.ps1
```

3. 手动销毁全部 Terraform 资源：

```powershell
.\.bin\terraform.exe destroy -auto-approve -input=false -var=enable_ha=true
```

## 六、后续建议

1. 如需长期运行，建议将 `ALLOWED_HOSTS='*'` 收敛为 ALB 域名白名单。
2. 在 CI 中增加部署后探测（前端 200 + API 200 双条件）。
3. 建议补充 CloudWatch 告警（TG unhealthy、EC2 状态、5xx 比例）。

---

报告结论：**本次部署验收通过（PASS）**。